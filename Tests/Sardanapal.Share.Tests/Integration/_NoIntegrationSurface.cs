// Sardanapal.Share is a pure-logic leaf package with no external runtime dependencies
// (no database, message broker, or cache), so it has no integration test surface.
// All Share tests live under Unit/.
namespace Sardanapal.Share.Tests.Integration;
